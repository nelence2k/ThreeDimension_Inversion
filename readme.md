# 正常改代码
改代码，commit,push
# 做新功能
在main 新建分支，从分支开发，测试通过，merge回main,push


# check out
如果checkout到一个历史提交点，而不是分支名，那么git就会变成，只是“临时站在这个历史版本上看一眼”，不属于任何分支，叫做detached head
一般checkout到某一个分支，你在这个分支上的提交就会跟在这个分支后面，

# U含义
U表示 untracked  新改动的文件还未加入暂存区